# Leave as SQLite for zero-setup local dev, or point to a Postgres+PostGIS
# instance (e.g. a free Supabase project — PostGIS is enabled by default there)
DATABASE_URL=sqlite:///./darukaa.db
# DATABASE_URL=postgresql://user:password@host:5432/dbname

SECRET_KEY=replace-with-a-long-random-string
